# git add is used to add the file (untracked file(source code) to tracked file(staging area))
# git commit(tracked file to saved /committed file(local repo))
#git push (saved/commited file to central repo)
#git pull(central repo to main source code it will be pulled)


# git init
# git config --user.name 
#git status
#git add filename
#git rm --cached filename